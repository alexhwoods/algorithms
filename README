Sometimes I'll do algorithms for fun. It can be a good way to practice a new language.

To run these, I run it in the simplest way possible.

### Go
```
go run main.go
```

### JavaScript
I just use Node to run the JS files.
```
node file.js
```


### Kotlin
Something like:
```
kotlinc minSwaps.kt -include-runtime -d temp.jar \
    && java -jar temp.jar \
    && rm temp.jar
```
Love you compiled languages 😘.


TODO: Get a nice Docker file you can run these things in.